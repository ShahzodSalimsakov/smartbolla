import * as types from '../types'
const initialState = {
  posts: [],

}
export const postReducer = (state=initialState, action,) => {
  switch (action.type) {
    default:
      case:
      return state
  }
}